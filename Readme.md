# Практическое занятие 1  
**Работа с компонентами: Button, Label, Memo, TrackBar, ProgressBar, графика**  
Среда: **Lazarus (Free Pascal / Object Pascal), Python (PyCharm), С#, С++ (Visual Studio)**

---

## 🔐 ВАЖНО: индивидуальные параметры

Чтобы исключить списывание, **все текстовые сообщения должны содержать ваше имя**:

> Вместо «Привет, Мир» → **«Привет, [ВАШЕ ИМЯ]!»**  
> Пример: `Привет, Анна!`

❗ Работы без вашего имени — **не засчитываются**.

---

## 📌 Основные задания (обязательные)

Выполните в **одном проекте** следующие задачи:

1. **Кнопка → Label**  
   По нажатию кнопки в элементе `Label` отображается надпись: `Привет, [ИМЯ]!`

2. **Кнопка → Memo**  
   По нажатию другой кнопки в поле `Memo` (или `TMemo`) добавляется строка: `Привет, [ИМЯ]!`

3. **Счётчик нажатий**  
   Кнопка увеличивает число в отдельном `Label` или `Edit` на 1 при каждом нажатии (начальное значение — 0).

4. **TrackBar → ProgressBar**  
   Значение `TrackBar` напрямую управляет `ProgressBar`.

5. **Вертикальный ProgressBar**  
   Измените ориентацию `ProgressBar` на **вертикальную** (свойство `Orientation = pbVertical`).  
   Используйте его для отображения, например, уровня громкости или топлива.

6. **Вывод значения TrackBar**  
   Отображайте текущее значение `TrackBar` в отдельном `Label` или `Edit`.

7. **Случайные числа + ProgressBar**  
   - `TrackBar` задаёт **максимальное значение** диапазона случайных чисел (от 0 до `TrackBar.Position`).  
   - По кнопке **«Сброс»** генерируется новое случайное число и отображается в `ProgressBar`.

8. **Цвет по RGB**  
   Тремя `TrackBar` задавайте значения **R, G, B** (0–255).  
   Отображайте:
   - Цветной прямоугольник (например, на `TPaintBox` или `TShape`),
   - Текст с кодом цвета в формате: `RGB(120, 45, 200)`.

---

## 🌟 Дополнительные задания (на выбор)

Выполните **любые** из следующих задач для получения доп. баллов:

- [ ] Установить **абстрактный фон** формы (без изображений животных, лиц и т.п.).  
      Используйте `TImage` или `Form.Color`.
- [ ] Кнопки **«Увеличить» / «Уменьшить»** — изменяют размер шрифта в `Label` или `Memo`.
- [ ] Изменить **цвет полосы** `ProgressBar` (например, на красный или синий).
- [ ] `TrackBar` управляет **радиусом круга**, отображаемого на форме.
- [ ] `TrackBar` управляет **эксцентриситетом эллипса** (степенью сжатия).
- [ ] `TrackBar` задаёт **количество кругов** (например, имитация пикового индикатора уровня).
- [ ] Два `TrackBar` задают **ширину и высоту прямоугольника** на форме.

> 💡 Дизайн должен быть **аккуратным**: выравнивание, подписи к компонентам, отступы.

---

## 📁 Структура проекта

- Задания реализуются в **разных проектах**.
- Форма должна быть **понятной и удобной** — не «куча компонентов в углу».
- Используйте `TPanel`, `TGroupBox`, подписи (`Caption`) для группировки.

---

## 📤 Сдача работы

1. Загрузите **весь проект** в репозиторий:
   - `.lpi`, `.lpr`, `.lfm`, `.pas` и другие файлы проекта.
2. Убедитесь, что проект **компилируется и запускается**.
3. Сделайте **несколько осмысленных коммитов** (см. [COMMIT_GUIDE.md](COMMIT_GUIDE.md)).

> ⚠️ Последний коммит — до дедлайна.  
> Работы без индивидуального имени — **0 баллов**.

---

## 🛑 Запрещено

- Использовать чужие проекты без изменений.
- Копировать код из интернета без понимания.
- Использовать изображения с котиками, мемами, логотипами.

---

Удачи! Помните: цель — **научиться управлять компонентами**, а не просто «сдать».
